namespace MaterialDialogs
{
    public enum StackingBehavior
    {
        //Action buttons stacked vertically if necessary for them to fit in the dialog.
        Adaptive,
        //Action buttons always atacked vertically.
        Always,
        //Action buttons are never stacked.
        Never
    }
}